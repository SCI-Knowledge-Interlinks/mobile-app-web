{
  "name": "prawaas-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "db:init": "node src/database/initDatabase.js",
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "npm ci --omit=dev && mkdir -p dist && touch dist/.keep"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "firebase-admin": "^14.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "mysql2": "^3.22.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
