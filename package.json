{
  "name": "prawaas-web",
  "version": "1.0.0",
  "private": true,
  "description": "Prawaas frontend — Expo web export for Hostinger static hosting",
  "scripts": {
    "start": "npm --prefix frontend start",
    "web": "npm --prefix frontend run web",
    "build": "npm --prefix frontend ci && npm --prefix frontend run build:web && node scripts/verify-dist.mjs",
    "build:web": "npm --prefix frontend run build:web && node scripts/verify-dist.mjs"
  }
}
